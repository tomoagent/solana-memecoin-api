services:
  - type: web
    name: solana-memecoin-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn real_api_final:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.7