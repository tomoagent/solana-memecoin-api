services:
  - type: web
    name: tomo-webhook-automation
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn toku_webhook_handler:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"